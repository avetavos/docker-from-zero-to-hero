---
title: 'การ Publish และ Expose port'
description: 'วิธีทำให้ port ของ container เข้าถึงได้จาก host ด้วย -p และทำไม EXPOSE เพียงอย่างเดียวจึงไม่พอ'
sidebar:
  order: 4
---

import PlayWithDocker from '../../../../components/PlayWithDocker.tsx';
import Callout from '../../../../components/Callout.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

## EXPOSE กับ -p ต่างกันอย่างไร

Dockerfile ของคุณอาจมีบรรทัดเช่น `EXPOSE 80` สิ่งนี้บอกผู้อ่าน (และเครื่องมือ) ว่า application ภายใน container ฟังที่ port 80 แต่ **ไม่ได้ publish port นั้นสู่ภายนอก**

หากต้องการให้ host หรือโลกภายนอกสามารถเข้าถึง port ของ container ได้จริง คุณต้องใช้ flag `-p` (หรือ `--publish`) เมื่อรัน container

## Syntax การ Publish port

รูปแบบพื้นฐานคือ `-p <host-port>:<container-port>`:

```bash
docker run -d --name webserver -p 8080:80 nginx:alpine
```

สิ่งนี้ผูก port 8080 ของ host กับ port 80 ของ container การ request ที่มาถึง `localhost:8080` จะถูกส่งต่อไปยัง port 80 ของ container โดย Docker

## ดูรายการ Port Mapping

ตรวจสอบว่า port ใดถูก publish สำหรับ container:

```bash
docker port webserver
```

ผลลัพธ์จะแสดง: `80/tcp -> 0.0.0.0:8080`

## ทดสอบ Published Port

เมื่อ publish แล้ว คุณสามารถเข้าถึงได้จาก host:

```bash
curl http://localhost:8080
```

## ผูกกับ Interface เฉพาะ

โดยค่าเริ่มต้น Docker จะ bind กับทุก interface (`0.0.0.0`) หากต้องการผูกเฉพาะ loopback interface:

```bash
docker run -d --name local-only -p 127.0.0.1:9090:80 nginx:alpine
```

วิธีนี้ทำให้ service เข้าถึงได้เฉพาะจาก machine นั้นเอง ไม่สามารถเข้าถึงได้จาก network ภายนอก

## Publish หลาย Port พร้อมกัน

ใช้ flag `-p` หลายครั้งเพื่อ publish หลาย port:

```bash
docker run -d --name multi -p 8080:80 -p 8443:443 nginx:alpine
```

## ลงมือปฏิบัติ: Publishing Ports

export const publishPortsCode = `# Publish port 80 of nginx to host port 8080
docker run -d --name webserver -p 8080:80 nginx:alpine

# List port mappings
docker port webserver

# Curl the published port
curl http://localhost:8080

# Bind to loopback only
docker run -d --name local-only -p 127.0.0.1:9090:80 nginx:alpine

# Multiple ports
docker run -d --name multi -p 8080:80 -p 8443:443 nginx:alpine

# Cleanup
docker stop webserver local-only multi && docker rm webserver local-only multi`;

<PlayWithDocker client:visible code={publishPortsCode} label="ทดสอบการ publish port" />

<Callout title="EXPOSE คือเอกสาร, -p คือการผูก port จริง">
EXPOSE ใน Dockerfile บอกผู้อ่านว่า application ใช้ port ใด แต่ไม่ได้ publish จริง มีเพียง -p (หรือ ports: ใน Compose) เท่านั้นที่ทำให้ port เข้าถึงได้จากภายนอก container
</Callout>

## Trade-offs

| ตัวเลือก | Benefit | Cost |
|---|---|---|
| Publish ไปที่ `0.0.0.0` (ทุก interface) | เข้าถึงได้จากทุกเครื่องในเครือข่าย เหมาะกับ service ที่ต้องเปิดสู่ภายนอกจริง | เปิดช่องเข้าถึงกว้างเกินจำเป็นถ้า service นั้นควรใช้แค่ภายในเครื่อง |
| Publish ไปที่ `127.0.0.1` เท่านั้น | จำกัดการเข้าถึงเฉพาะจาก host เดียวกัน ปลอดภัยกว่า | เข้าถึงจากเครื่องอื่นในเครือข่ายไม่ได้เลย ต้องแก้ flag ถ้าจะเปิดออกภายหลัง |

## Common Mistakes

- เข้าใจผิดว่า `EXPOSE` ใน Dockerfile publish port ออกมาจริง ทั้งที่มันเป็นแค่เอกสารบอกว่า container ฟัง port ไหน ต้องใช้ `-p`/`--publish` เท่านั้นถึงจะ map port ออกมาให้เข้าถึงได้จริง
- Publish port ไปที่ `0.0.0.0` โดยไม่ตั้งใจสำหรับ service ที่ควรเข้าถึงได้แค่จาก localhost เช่น database หรือ admin panel ทำให้เปิดสู่เครือข่ายภายนอกโดยไม่จำเป็น
- Publish port เดียวกันซ้ำจากหลาย container บน host เดียว ทำให้ `docker run` ล้มเหลวเพราะ port ถูกจองไปแล้วโดย container ตัวอื่น

> 💡 Real World Example
>
> Pattern ทั่วไปในการ deploy คือ publish port แค่จาก reverse proxy ตัวเดียว (เช่น Traefik หรือ nginx) ที่ `0.0.0.0:80` และ `0.0.0.0:443` ส่วน backend service อื่น ๆ ไม่ publish port เลย แต่คุยกันผ่าน internal network ภายใน ทำให้พื้นที่ที่เปิดสู่ภายนอกมีแค่จุดเดียว

export const quizPublishPorts = [
  {
    q: "คำสั่ง EXPOSE ใน Dockerfile จริงๆ แล้วทำอะไร?",
    options: [
      "สร้าง firewall rule บน host เพื่ออนุญาต traffic เข้า container",
      "Bind host port กับ container port",
      "บันทึก port ที่ application ฟังอยู่แต่ไม่ได้ publish จริง",
      "ส่ง traffic ทั้งหมดบน port นั้นจาก host ไปยัง container โดยอัตโนมัติ"
    ],
    answer: 2
  },
  {
    q: "คุณรัน: docker run -p 9000:3000 myapp ข้อใดต่อไปนี้อธิบาย port mapping ได้ถูกต้อง?",
    options: [
      "Host port 3000 map ไปยัง container port 9000",
      "Host port 9000 map ไปยัง container port 3000",
      "ทั้ง host และ container ใช้ port 9000",
      "ทั้ง host และ container ใช้ port 3000"
    ],
    answer: 1
  },
  {
    q: "จะจำกัด published port ให้เข้าถึงได้เฉพาะจาก local machine (loopback interface) ได้อย่างไร?",
    options: [
      "docker run -p 80 --internal myapp",
      "docker run --restrict-external -p 80:80 myapp",
      "docker run -p 127.0.0.1:8080:80 myapp",
      "docker run -p localhost:8080:80 myapp"
    ],
    answer: 2
  },
  {
    q: "คำสั่งใดแสดงรายการ port mapping ของ container ชื่อ 'webserver'?",
    options: [
      "docker inspect webserver --ports",
      "docker ps -p webserver",
      "docker network inspect webserver",
      "docker port webserver"
    ],
    answer: 3
  }
];

<Quiz client:visible id="networking/publish-ports" questions={quizPublishPorts} />

<ProgressTracker client:visible id="networking/publish-ports" />
